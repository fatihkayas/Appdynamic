apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "palo-alto.fullname" . }}-test-connection"
  labels:
    {{- include "palo-alto.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "palo-alto.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
